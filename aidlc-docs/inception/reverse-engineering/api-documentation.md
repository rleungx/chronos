# API Documentation

No public API or protobuf contract changes are planned. Existing `AllocateTimestamps` request deadlines and error mappings remain compatible. New observability is additive Prometheus telemetry only.
